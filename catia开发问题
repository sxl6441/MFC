宋小亮_2020-6-6
CAA中的零件/组件/特征有没有像CATICGMObject下GetPersistentTag ()重新加载后也不变ID标识：
1，如果有的话能否通过从组件ROOT->当前零件的各自的ID组合(如156,16,23)得到CATPathElement
2，如果没有我只知道各自实例名称怎么通过初始化得到CATPathElement，有没有像CREO一样的构造函数
目的：
当程序在一次进程中检查到了某个零件的特征或几何问题，下次重新加载模型后要反向定位到对应的对象上，如果用名称定位也不好(因名称要能会改，不稳定)

戈飞_2020-6-18图纸中的尺寸或视图更新前后的状态用什么API获得？其它对象也详解一下更新/状态的API用法，现在用 CATISpecUpdate::IsUpToDate 发现刷新前后状态不会变

戈飞_2020-6-181，钣金怎么用API实现实际展平。
2，获得对应的展平或折回几何(发现API有参数，待验证)

宋小亮_2020-6-20怎么实现在创建任意特征前或后触发已定义的事件函数，并能得到当前的特征对象，后续要对对象进行名称/类型/几何的获取或修改。

宋小亮_2020-6-20怎么实现在装配零件前或后触发已定义的事件函数，并能得到当前的被装配组件对象和装配对象，后续要对对象进行名称/类型的获取进行后续自动化相关功能。

宋小亮_2020-6-20在MFC窗中类下怎么实现对象的交互选择？用标准的代理无法选择并回调？
代理选择能不能有阻塞方式的？

宋小亮_2020-6-201，CAA的原生窗口设计器的控件怎么实现继承后的功能扩展，如在列表控件的每个表格中增加颜色控制，表格中增加子控件如BUTTON,CHECKBOX,COMBOX…等等
2，没找到树形控件。

宋小亮_2020-6-201，钣金模块的常用API怎么使用：比如怎么得到像Proe的
绿面(基面)和对面，
折弯R，
折弯角度，
板材长度，
希望能给些代码例子。

戈飞_2020-7-31-Drawing的尺寸丢失参照了怎么判断到这个状态。
2-尺寸的参考怎么获得？

宋小亮_2020-7-91-用AUTOMATION的
SelectElement2/3
如果当前组件下有同一个零件的两个实例，选择结果永远是指向同一个实例上的对象?如右图
2-如果选择的是Facet得到CATIBRepAccess_var特征化后发现也对不上实际的实例,怎么实现外部参考得到正确的面/边特征？

宋小亮_2020-7-10CATIContainer * pIContainer = NULL;
rc = pIContainerOfDocumentOnDoc->GetBRepContainer(pIContainer);
得到的CATIContainer对应的是那个CATI接口？
怎么用GetBRepContainer这个容器下的函数？

戈飞_2020-7-22怎么得特征自已对应的几何？
现在发现如果当前有1，2，3四个特征，现找特征3对应的面时，都是包括1，2特征的面。

李俊_2020-7-24各种焊接类型获取，角焊：如果是断续焊获取焊点数量，如果非断续焊获取焊接长度，坡口焊：如果是断续焊获取焊点数量，如果非断续焊获取焊接长度，塞焊：获取焊点数量和焊缝深度，点焊：获取焊点数量。

宋小亮_2020-8-6CAA二次开发或手动进行干涉/距离执行完后，怎么实现不要在模型 树的Applications中显示那些结果。

song_2020-8-271-干涉和间隙检查怎么设置排除(面组/坐标/基准/曲线…?)
2-怎么把指定的零件或几何（其实就是一个包络）定位到/并适应窗比例 到屏幕中心？
